# Baza danych — MUSI być taka sama ścieżka jak w swi.campas.pl
DATABASE_URL=sqlite+aiosqlite:////home/user/app/obozlog/backend/obozlog.db

# JWT — MUSI być identyczny jak w swi.campas.pl
JWT_SECRET=wygeneruj-nowy-sekret-openssl-rand-hex-32

# Frontend
FRONTEND_URL=https://api.campas.pl
ALLOWED_ORIGINS=https://api.campas.pl,https://swi.campas.pl

# SMTP (Resend)
SMTP_HOST=smtp.resend.com
SMTP_PORT=587
SMTP_USER=resend
SMTP_PASSWORD=re_TWOJ_KLUCZ_RESEND
SMTP_FROM=noreply@campas.pl
